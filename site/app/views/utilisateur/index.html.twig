{% extends '_template/base.html.twig' %}

{% block body %}
<table class="table table-striped table-hover">
    <thead>
    <tr>
        <th scope="col">#ID</th>
		<th scope="col">Numéro étuiant</th>
		<th scope="col">Admin</th>
        <th scope="col">Nom complet</th>
        <th scope="col">Email</th>
        <th scope="col">Actions</th>
    </tr>
    </thead>
    <tbody>
    {% for utilisateur in utilisateurs %}
        <tr>
            <td> {{ utilisateur.id }} </td>
			<td> {{ utilisateur.numEtu }} </td>
			<td> {{ utilisateur.estAdmin ? 'Vrai' : '' }} </td>
            <td> {{ utilisateur.prenom ~ ' ' ~ utilisateur.nom }} </td>
            <td> {{ utilisateur.email }} </td>
            <td>
                <a href="utilisateur_update.php?id={{ utilisateur.id }}" class="btn btn-warning btn-sm"><i class="fa fa-edit"></i></a>
                <a href="utilisateur_delete.php?id={{ utilisateur.id }}" class="btn btn-danger btn-sm"><i class="fa fa-trash"></i></a>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
<div class="d-flex flex-row-reverse">
    <a href="utilisateur_create.php" class="btn btn-success">
        <i class="fa fa-plus"></i> Ajouter un utilisateur
    </a>
</div>
{% endblock %}